# @package _global_
defaults:
  - /forcefield/options/runtime@forcefield.options
  - override /seed_bias: solvent_ready
  - _self_

forcefield:
  options:
    # Relaxation restraint and protocol controls
    # Used mainly for seed-generation and periodic handoff cleanup defaults.
    hbond_k: 0.0
