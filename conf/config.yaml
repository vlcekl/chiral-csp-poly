defaults:
  - topology/backbone: amylose
  - topology/selector: 35dmpc
  - structure/helix: amylose_4_3_derivatized
  - forcefield/options: runtime
  - ordering: basic
  - forcefield: mixing_rules
  - qc: basic
  - multi_opt: default
  - periodic_handoff: default
  # Select a named commercial CSP with e.g. `phase=chiralpak_ad` or `phase=chiralcel_oz`.
  - optional phase: null
  - _self_

output:
  dir: outputs
  export_formats: [pdb, sdf, amber]

docking:
  box_buffer_A: 6.0
  window_residues: null
